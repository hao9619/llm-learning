# Qwen2.5-3B 本地部署与 QLoRA 微调技术文档

## 1. 项目概述

本文档记录一次在本地 Ubuntu 22 环境中部署、推理和微调大语言模型的完整学习流程。

目标模型：

```text
Qwen/Qwen2.5-3B-Instruct
```

主要学习内容包括：

1. 使用 Ollama 快速部署本地大语言模型；
2. 使用 HuggingFace Transformers 手动加载模型并推理；
3. 使用 PyTorch 理解张量、GPU 与模型推理流程；
4. 使用 bitsandbytes 对模型进行 4-bit 量化；
5. 使用 PEFT 和 LoRA 进行参数高效微调；
6. 使用 QLoRA 在低显存显卡上微调大语言模型；
7. 使用 TRL 的 SFTTrainer 进行监督微调；
8. 保存和加载 LoRA adapter；
9. 分析推理中的幻觉问题与微调风险。

本项目硬件环境：

```text
系统：Ubuntu 22
显卡：RTX 4060 8GB
内存：32GB
硬盘：约 199GB，总剩余空间约 100GB
```

在该配置下，推荐进行：

```text
Qwen2.5-3B-Instruct 推理
Qwen2.5-3B-Instruct 4-bit 量化推理
Qwen2.5-3B-Instruct QLoRA 微调
```

不推荐进行：

```text
7B 以上模型全参数微调
13B 以上模型本地训练
超长上下文训练
大型多模态模型全量训练
```

---

# 2. 整体技术路线

本次学习路线分为三个阶段。

```text
阶段一：Ollama 快速部署
    ↓
阶段二：Transformers 手动加载模型推理
    ↓
阶段三：QLoRA 微调并加载 LoRA adapter 推理
```

对应关系如下：

| 阶段 | 工具 | 目的 |
|---|---|---|
| Ollama | Ollama / llama.cpp / GGUF | 快速部署与体验本地模型 |
| 手写推理 | PyTorch / Transformers | 理解模型加载、tokenizer、generate |
| QLoRA 微调 | PEFT / bitsandbytes / TRL | 学习参数高效微调流程 |
| LoRA 推理 | PEFT / Transformers | 加载微调 adapter 并生成回答 |

---

# 3. Ollama 技术介绍

## 3.1 Ollama 是什么

Ollama 是一个本地大语言模型运行和管理工具。

它主要用于：

```text
下载模型
管理模型
运行模型
提供本地 API
支持命令行聊天
快速部署本地 LLM
```

例如：

```bash
ollama run qwen2.5:3b
```

这条命令会完成：

1. 检查本地是否存在 `qwen2.5:3b`；
2. 如果不存在，则自动下载模型；
3. 加载模型；
4. 启动交互式命令行对话。

---

## 3.2 Ollama 与开源性

Ollama 本身是 GitHub 上的开源项目，核心使用 Go 语言实现。

但是需要注意：

```text
Ollama 工具开源
通过 Ollama 下载的模型不一定都完全开源
不同模型有不同许可证
```

例如：

| 模型 | 许可证情况 |
|---|---|
| Qwen | 有自己的开源许可证 |
| Llama | 有 Meta 自己的 License |
| Mistral | 有自己的许可证 |
| Gemma | 有 Google 的许可证 |

---

## 3.3 Ollama 底层原理

Ollama 底层主要依赖 llama.cpp 生态和 GGUF 模型格式。

大致结构如下：

```text
Ollama
  ↓
模型管理、命令行接口、本地 HTTP API
  ↓
llama.cpp 推理后端
  ↓
GGUF 量化模型
  ↓
CPU / GPU 执行推理
```

Ollama 的特点是封装程度高，适合部署和体验，但不适合作为深入学习训练原理的唯一工具。

---

## 3.4 Ollama 常用命令

### 安装 Ollama

```bash
curl -fsSL https://ollama.com/install.sh | sh
```

作用：

```text
下载 Ollama 安装脚本
执行安装
注册 ollama 命令
配置本地服务
```

---

### 查看版本

```bash
ollama --version
```

作用：

```text
确认 Ollama 是否安装成功
显示当前 Ollama 版本
```

---

### 运行模型

```bash
ollama run qwen2.5:3b
```

作用：

```text
下载并运行 Qwen2.5 3B 模型
进入交互式对话界面
```

---

### 查看本地模型

```bash
ollama list
```

作用：

```text
列出已经下载到本地的模型
显示模型名称、ID、大小等信息
```

---

### 调用 Ollama API

```bash
curl http://localhost:11434/api/generate -d '{
  "model": "qwen2.5:3b",
  "prompt": "请解释什么是大语言模型。",
  "stream": false
}'
```

作用：

```text
通过 HTTP 请求调用本地模型
返回 JSON 格式的生成结果
```

参数说明：

| 参数 | 作用 |
|---|---|
| model | 指定调用哪个模型 |
| prompt | 用户输入文本 |
| stream | 是否流式输出 |

`stream: false` 表示等待完整回答生成后一次性返回。

---

# 4. Python 调用 Ollama API

代码：

```python
import requests

url = "http://localhost:11434/api/generate"

payload = {
    "model": "qwen2.5:3b",
    "prompt": "请解释什么是 QLoRA，并说明它为什么省显存。",
    "stream": False
}

response = requests.post(url, json=payload)
response.raise_for_status()

data = response.json()
print(data["response"])
```

---

## 4.1 requests 库

```python
import requests
```

`requests` 是 Python 中常用的 HTTP 请求库。

在这里它的作用是：

```text
向 Ollama 本地 API 发送 POST 请求
接收模型返回的 JSON 数据
```

---

## 4.2 url

```python
url = "http://localhost:11434/api/generate"
```

含义：

```text
localhost 表示本机
11434 是 Ollama 默认服务端口
/api/generate 是文本生成接口
```

---

## 4.3 payload

```python
payload = {
    "model": "qwen2.5:3b",
    "prompt": "请解释什么是 QLoRA，并说明它为什么省显存。",
    "stream": False
}
```

这是请求体，告诉 Ollama：

```text
使用哪个模型
输入什么 prompt
是否流式返回
```

---

## 4.4 requests.post

```python
response = requests.post(url, json=payload)
```

作用：

```text
向 Ollama 服务发送一个 POST 请求
请求内容自动以 JSON 格式发送
```

---

## 4.5 response.raise_for_status

```python
response.raise_for_status()
```

作用：

```text
如果 HTTP 请求失败，则抛出异常
例如 404、500、连接失败等
```

---

## 4.6 response.json

```python
data = response.json()
```

作用：

```text
把返回的 JSON 字符串解析成 Python 字典
```

---

# 5. Conda 与 Python 环境

## 5.1 为什么使用 Conda

Conda 用于创建独立 Python 环境。

本项目中建议创建：

```bash
conda create -n qwen_lora python=3.10 -y
```

原因：

```text
避免污染系统 Python
方便管理 PyTorch、Transformers、bitsandbytes 等依赖
方便复现实验环境
```

---

## 5.2 激活环境

```bash
conda activate qwen_lora
```

作用：

```text
进入 qwen_lora 环境
后续安装的库和运行的 Python 都属于该环境
```

---

## 5.3 安装 PyTorch

```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

作用：

```text
安装支持 CUDA 12.1 的 PyTorch
使 Python 可以调用 NVIDIA GPU 进行计算
```

其中：

| 包 | 作用 |
|---|---|
| torch | PyTorch 核心库 |
| torchvision | 计算机视觉工具库 |
| torchaudio | 音频相关工具库 |

---

## 5.4 检查 GPU

```bash
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
```

作用：

```text
检查 PyTorch 是否安装成功
检查 CUDA 是否可用
检查当前 GPU 名称
```

如果输出：

```text
True
NVIDIA GeForce RTX 4060
```

说明 GPU 可用。

---

# 6. HuggingFace Transformers 技术介绍

## 6.1 Transformers 是什么

Transformers 是 HuggingFace 提供的深度学习模型库。

它支持：

```text
加载预训练模型
加载 tokenizer
文本生成
模型微调
模型保存与上传
多种模型架构
```

在本项目中主要用到：

```python
from transformers import AutoTokenizer, AutoModelForCausalLM
```

---

## 6.2 AutoTokenizer

```python
tokenizer = AutoTokenizer.from_pretrained(
    model_name,
    trust_remote_code=True
)
```

作用：

```text
根据模型名称自动加载对应 tokenizer
把文本转换成模型能理解的 token ID
把模型输出的 token ID 解码回文本
```

---

### Tokenizer 的原理

大语言模型不能直接处理自然语言字符串。

它需要先把文本变成整数序列：

```text
"请解释 LoRA" 
    ↓ tokenizer
[12345, 6789, 9012, ...]
```

这些整数称为：

```text
token IDs
```

模型内部再把 token IDs 映射为向量。

---

## 6.3 AutoModelForCausalLM

```python
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    torch_dtype=torch.float16,
    device_map="auto",
    trust_remote_code=True
)
```

作用：

```text
自动加载因果语言模型
用于自回归文本生成
```

CausalLM 的含义是：

```text
Causal Language Model
因果语言模型
```

它的训练目标是：

```text
根据前面的 token 预测下一个 token
```

例如：

```text
输入：LoRA 是一种
预测：参数
再输入：LoRA 是一种 参数
预测：高效
```

---

## 6.4 from_pretrained

```python
AutoModelForCausalLM.from_pretrained(model_name)
```

作用：

```text
从 HuggingFace Hub 或本地缓存加载预训练模型权重
```

如果本地没有模型，会自动下载。

---

## 6.5 torch_dtype

```python
torch_dtype=torch.float16
```

作用：

```text
使用 FP16 半精度加载模型
减少显存占用
加快 GPU 推理速度
```

常见精度：

| 精度 | 位宽 | 特点 |
|---|---:|---|
| FP32 | 32-bit | 精度高，显存占用大 |
| FP16 | 16-bit | 显存更省，GPU 推理常用 |
| BF16 | 16-bit | 数值范围更好，部分显卡支持 |
| INT8 | 8-bit | 更省显存 |
| INT4 | 4-bit | 极省显存，常用于 QLoRA |

---

## 6.6 device_map

```python
device_map="auto"
```

作用：

```text
自动决定模型各层放在 GPU 还是 CPU
```

对于单卡用户，一般会尽量把模型放到：

```text
cuda:0
```

如果显存不够，可能部分层被放到 CPU。

---

## 6.7 trust_remote_code

```python
trust_remote_code=True
```

作用：

```text
允许加载模型仓库中自定义的模型代码
```

部分模型需要自定义架构代码，开启该参数才能正常加载。

注意：

```text
这意味着你信任该模型仓库中的代码
建议只对可信模型使用
```

---

# 7. Transformers 推理代码解析

完整推理流程：

```python
import torch
from transformers import AutoTokenizer, AutoModelForCausalLM


def main():
    model_name = "Qwen/Qwen2.5-3B-Instruct"

    print("Loading tokenizer...")
    tokenizer = AutoTokenizer.from_pretrained(
        model_name,
        trust_remote_code=True
    )

    print("Loading model...")
    model = AutoModelForCausalLM.from_pretrained(
        model_name,
        torch_dtype=torch.float16,
        device_map="auto",
        trust_remote_code=True
    )

    model.eval()

    messages = [
        {
            "role": "system",
            "content": "你是一个耐心的人工智能和深度学习助教。"
        },
        {
            "role": "user",
            "content": "请用通俗语言解释什么是 Transformer 的注意力机制。"
        }
    ]

    text = tokenizer.apply_chat_template(
        messages,
        tokenize=False,
        add_generation_prompt=True
    )

    inputs = tokenizer(
        [text],
        return_tensors="pt"
    ).to(model.device)

    print("Generating...")

    with torch.no_grad():
        outputs = model.generate(
            **inputs,
            max_new_tokens=512,
            do_sample=True,
            temperature=0.7,
            top_p=0.9,
            repetition_penalty=1.05
        )

    generated_ids = outputs[0][inputs["input_ids"].shape[-1]:]
    response = tokenizer.decode(
        generated_ids,
        skip_special_tokens=True
    )

    print("\n模型回答：")
    print(response)


if __name__ == "__main__":
    main()
```

---

## 7.1 model.eval

```python
model.eval()
```

作用：

```text
将模型切换到推理模式
```

在推理模式下：

```text
Dropout 会关闭
BatchNorm 使用固定统计量
模型输出更稳定
```

对于 LLM，主要影响是关闭 Dropout。

---

## 7.2 messages

```python
messages = [
    {
        "role": "system",
        "content": "你是一个耐心的人工智能和深度学习助教。"
    },
    {
        "role": "user",
        "content": "请用通俗语言解释什么是 Transformer 的注意力机制。"
    }
]
```

这是聊天模型常用的对话格式。

角色包括：

| role | 含义 |
|---|---|
| system | 系统提示，定义助手身份和行为 |
| user | 用户输入 |
| assistant | 模型回答 |

---

## 7.3 apply_chat_template

```python
text = tokenizer.apply_chat_template(
    messages,
    tokenize=False,
    add_generation_prompt=True
)
```

作用：

```text
把结构化 messages 转换成模型训练时使用的对话文本格式
```

不同模型的对话模板不同。

例如 Qwen 的模板可能类似：

```text
<|im_start|>system
你是一个耐心的人工智能和深度学习助教。
<|im_end|>
<|im_start|>user
请解释什么是注意力机制。
<|im_end|>
<|im_start|>assistant
```

`add_generation_prompt=True` 的作用是：

```text
在末尾添加 assistant 开始标记
提示模型从这里开始生成回答
```

---

## 7.4 tokenizer 编码

```python
inputs = tokenizer(
    [text],
    return_tensors="pt"
).to(model.device)
```

作用：

```text
将文本转换成 PyTorch Tensor
并移动到模型所在设备
```

输出通常包括：

| 字段 | 作用 |
|---|---|
| input_ids | token ID 序列 |
| attention_mask | 标记哪些 token 是有效输入 |

---

## 7.5 torch.no_grad

```python
with torch.no_grad():
```

作用：

```text
关闭梯度计算
减少显存占用
加快推理速度
```

推理时不需要反向传播，所以应使用 `torch.no_grad()`。

---

## 7.6 model.generate

```python
outputs = model.generate(
    **inputs,
    max_new_tokens=512,
    do_sample=True,
    temperature=0.7,
    top_p=0.9,
    repetition_penalty=1.05
)
```

这是文本生成核心函数。

参数说明：

| 参数 | 作用 |
|---|---|
| `**inputs` | 将 input_ids、attention_mask 传给模型 |
| max_new_tokens | 最多生成多少个新 token |
| do_sample | 是否使用随机采样 |
| temperature | 控制随机性 |
| top_p | nucleus sampling 截断概率 |
| repetition_penalty | 重复惩罚 |

---

### max_new_tokens

```python
max_new_tokens=512
```

表示最多生成 512 个新 token。

注意它不包括输入 token。

---

### do_sample

```python
do_sample=True
```

表示使用随机采样。

如果设置为：

```python
do_sample=False
```

则使用贪心解码，输出更稳定，但可能更死板。

---

### temperature

```python
temperature=0.7
```

控制生成随机性。

| temperature | 效果 |
|---:|---|
| 低，如 0.1 | 更确定、更保守 |
| 中，如 0.7 | 平衡 |
| 高，如 1.2 | 更随机、更发散 |

---

### top_p

```python
top_p=0.9
```

表示 nucleus sampling。

模型只在累计概率达到 0.9 的候选 token 中采样。

---

### repetition_penalty

```python
repetition_penalty=1.05
```

用于降低重复输出概率。

值越大，对重复越敏感。

---

## 7.7 截取新生成部分

```python
generated_ids = outputs[0][inputs["input_ids"].shape[-1]:]
```

`outputs` 包含：

```text
原始输入 token + 新生成 token
```

所以需要把原始输入部分切掉，只保留模型新回答。

---

## 7.8 tokenizer.decode

```python
response = tokenizer.decode(
    generated_ids,
    skip_special_tokens=True
)
```

作用：

```text
把 token IDs 转换回自然语言文本
```

`skip_special_tokens=True` 会去掉：

```text
<|im_start|>
<|im_end|>
<pad>
<eos>
```

等特殊 token。

---

# 8. 量化与 bitsandbytes

## 8.1 为什么需要量化

大模型参数很多，显存占用高。

例如 3B 参数模型，如果使用 FP16：

```text
3B 参数 × 2 bytes ≈ 6GB
```

再加上：

```text
KV cache
激活值
CUDA kernel
中间变量
LoRA 参数
优化器状态
```

8GB 显存会比较紧张。

因此需要量化。

---

## 8.2 什么是 4-bit 量化

4-bit 量化是把模型权重从 FP16/FP32 压缩到 4-bit 表示。

显存占用大约降低为原来的 1/4 到 1/3。

在 QLoRA 中，常用：

```text
NF4 4-bit 量化
```

---

## 8.3 BitsAndBytesConfig

代码：

```python
from transformers import BitsAndBytesConfig

bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_compute_dtype=torch.float16,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_use_double_quant=True
)
```

参数说明：

| 参数 | 作用 |
|---|---|
| load_in_4bit | 使用 4-bit 加载模型 |
| bnb_4bit_compute_dtype | 计算时使用的数据类型 |
| bnb_4bit_quant_type | 量化类型 |
| bnb_4bit_use_double_quant | 是否使用双重量化 |

---

### load_in_4bit

```python
load_in_4bit=True
```

表示模型权重以 4-bit 形式加载。

---

### bnb_4bit_compute_dtype

```python
bnb_4bit_compute_dtype=torch.float16
```

虽然权重以 4-bit 存储，但计算时仍会转换到 FP16 进行矩阵运算。

---

### bnb_4bit_quant_type

```python
bnb_4bit_quant_type="nf4"
```

NF4 是 NormalFloat 4-bit 的缩写，是 QLoRA 论文中常用的量化格式，适合近似正态分布的神经网络权重。

---

### bnb_4bit_use_double_quant

```python
bnb_4bit_use_double_quant=True
```

表示对量化常数再进行一次量化，进一步减少显存占用。

---

# 9. LoRA 原理

## 9.1 LoRA 是什么

LoRA 的全称是：

```text
Low-Rank Adaptation
```

中文常译为：

```text
低秩适配
```

它是一种参数高效微调方法。

---

## 9.2 LoRA 的核心思想

传统全参数微调会更新原模型中的大量权重矩阵。

假设某一层的权重为：

$$W$$

全参数微调会直接学习新的：

$$W'$$

而 LoRA 冻结原始权重：

$$W$$

只学习一个低秩增量：

$$\Delta W$$

令：

$$\Delta W = BA$$

其中：

```text
A 和 B 是两个低秩矩阵
它们的参数量远小于原始 W
```

最终等效为：

$$W' = W + \Delta W = W + BA$$

---

## 9.3 为什么 LoRA 省显存

LoRA 省显存的原因：

```text
不更新原模型大部分参数
只训练少量 adapter 参数
优化器状态只为 LoRA 参数保存
梯度也主要只为 LoRA 参数计算
```

因此相比全参数微调，显存占用大幅降低。

---

## 9.4 LoRA 中的 r

```python
r=8
```

`r` 是低秩矩阵的秩。

越大：

```text
可训练参数越多
表达能力越强
显存占用越高
过拟合风险越高
```

越小：

```text
显存更省
训练更快
表达能力较弱
```

对于 8GB 显存，常用：

```text
r = 4 或 8
```

---

## 9.5 LoRA alpha

```python
lora_alpha=16
```

LoRA 通常会对低秩更新进行缩放。

常见缩放形式：

$$\frac{\alpha}{r} BA$$

其中：

| 参数 | 作用 |
|---|---|
| alpha | 缩放系数 |
| r | LoRA 秩 |

`alpha` 越大，LoRA 对原模型影响越强。

---

## 9.6 LoRA dropout

```python
lora_dropout=0.05
```

作用：

```text
在 LoRA 分支中加入 dropout
减少过拟合
提高泛化能力
```

---

# 10. QLoRA 原理

## 10.1 QLoRA 是什么

QLoRA 可以理解为：

```text
Quantized LoRA
```

它结合了：

```text
4-bit 量化基础模型
LoRA adapter 微调
```

核心思想：

```text
把大模型主体量化为 4-bit 并冻结
只训练少量 LoRA 参数
```

---

## 10.2 QLoRA 与 LoRA 的区别

| 方法 | 基础模型 | 训练参数 | 显存占用 |
|---|---|---|---|
| 全参数微调 | FP16/FP32 | 全部参数 | 很高 |
| LoRA | FP16/FP32 | LoRA 参数 | 中等 |
| QLoRA | 4-bit | LoRA 参数 | 更低 |

---

## 10.3 QLoRA 适合你的原因

你的 RTX 4060 只有 8GB 显存。

QLoRA 可以让你在 8GB 显存上微调 3B 模型。

核心配置：

```python
load_in_4bit=True
per_device_train_batch_size=1
gradient_accumulation_steps=8
gradient_checkpointing=True
```

---

# 11. 微调数据格式

## 11.1 JSONL 格式

训练数据使用 JSONL 格式。

每一行是一个独立 JSON 对象。

例如：

```json
{"instruction": "请解释什么是 LoRA。", "input": "", "output": "LoRA 是 Low-Rank Adaptation 的缩写，是一种参数高效微调方法。"}
```

字段含义：

| 字段 | 作用 |
|---|---|
| instruction | 用户指令 |
| input | 可选补充输入 |
| output | 期望模型回答 |

---

## 11.2 为什么使用 JSONL

JSONL 优点：

```text
一行一个样本
便于增量添加
便于流式读取
适合大规模数据集
HuggingFace datasets 支持良好
```

---

## 11.3 数据质量的重要性

微调结果强烈依赖数据质量。

低质量数据可能导致：

```text
模型学到错误知识
幻觉变多
输出风格变差
灾难性遗忘
专业概念混淆
```

例如，如果模型回答：

```text
LoRA 是 Lightweight Recursive Operator Allocation
```

这是错误幻觉。

正确应为：

```text
LoRA = Low-Rank Adaptation
```

如果训练数据中没有足够高质量样本，微调不能保证修复模型错误。

---

# 12. QLoRA 训练代码解析

完整训练代码核心结构如下：

```python
import os
import torch
from datasets import load_dataset
from transformers import (
    AutoTokenizer,
    AutoModelForCausalLM,
    BitsAndBytesConfig,
    TrainingArguments,
    DataCollatorForLanguageModeling
)
from peft import (
    LoraConfig,
    get_peft_model,
    prepare_model_for_kbit_training
)
from trl import SFTTrainer
```

---

## 12.1 os

```python
import os
```

作用：

```text
处理文件路径
创建输出目录
展开用户目录 ~
```

例如：

```python
os.path.expanduser("~/llm-learning/data/train.jsonl")
```

会转换成：

```text
/home/用户名/llm-learning/data/train.jsonl
```

---

## 12.2 torch

```python
import torch
```

作用：

```text
提供张量计算
GPU 加速
FP16 类型
模型训练和推理基础
```

---

## 12.3 load_dataset

```python
from datasets import load_dataset
```

作用：

```text
从 JSONL 文件加载训练数据
转换为 HuggingFace Dataset 对象
```

代码：

```python
dataset = load_dataset(
    "json",
    data_files=data_path,
    split="train"
)
```

含义：

| 参数 | 作用 |
|---|---|
| "json" | 指定数据格式为 JSON/JSONL |
| data_files | 数据文件路径 |
| split="train" | 返回训练集 |

---

## 12.4 format_example 函数

```python
def format_example(example, tokenizer):
    instruction = example["instruction"]
    input_text = example.get("input", "")
    output = example["output"]

    if input_text:
        user_content = f"{instruction}\n\n补充输入：{input_text}"
    else:
        user_content = instruction

    messages = [
        {
            "role": "system",
            "content": "你是一个专业、严谨、耐心的人工智能学习助手。"
        },
        {
            "role": "user",
            "content": user_content
        },
        {
            "role": "assistant",
            "content": output
        }
    ]

    text = tokenizer.apply_chat_template(
        messages,
        tokenize=False,
        add_generation_prompt=False
    )

    return text
```

作用：

```text
把一条 instruction 数据转换成 Qwen 聊天模型需要的文本格式
```

---

### instruction

```python
instruction = example["instruction"]
```

读取用户指令。

---

### input_text

```python
input_text = example.get("input", "")
```

读取可选输入。

使用 `get` 的好处是：

```text
如果没有 input 字段，不会报错，而是返回空字符串
```

---

### output

```python
output = example["output"]
```

读取期望模型学习的回答。

---

### user_content

```python
if input_text:
    user_content = f"{instruction}\n\n补充输入：{input_text}"
else:
    user_content = instruction
```

作用：

```text
如果存在补充输入，就把 instruction 和 input 拼起来
如果没有补充输入，就只使用 instruction
```

---

### messages

```python
messages = [
    {"role": "system", "content": "..."},
    {"role": "user", "content": user_content},
    {"role": "assistant", "content": output}
]
```

作用：

```text
构造完整训练对话
包括系统提示、用户问题和助手答案
```

---

### add_generation_prompt=False

```python
add_generation_prompt=False
```

训练时不需要额外添加 assistant 生成提示，因为训练样本中已经包含 assistant 的完整回答。

推理时才需要：

```python
add_generation_prompt=True
```

---

## 12.5 加载 tokenizer

```python
tokenizer = AutoTokenizer.from_pretrained(
    model_name,
    trust_remote_code=True
)
```

作用：

```text
加载 Qwen2.5-3B-Instruct 对应 tokenizer
```

---

## 12.6 pad_token

```python
if tokenizer.pad_token is None:
    tokenizer.pad_token = tokenizer.eos_token
```

作用：

```text
如果 tokenizer 没有 padding token，就用 eos token 作为 padding token
```

为什么需要 padding：

```text
训练时一个 batch 内不同样本长度不同
需要补齐到相同长度
```

---

## 12.7 BitsAndBytesConfig

```python
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_compute_dtype=torch.float16,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_use_double_quant=True
)
```

作用：

```text
配置 QLoRA 所需的 4-bit 量化加载方式
```

---

## 12.8 加载 4-bit 模型

```python
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    quantization_config=bnb_config,
    device_map="auto",
    trust_remote_code=True
)
```

作用：

```text
以 4-bit 量化方式加载 Qwen2.5-3B-Instruct
```

---

## 12.9 use_cache

```python
model.config.use_cache = False
```

作用：

```text
关闭 KV cache
避免训练时与 gradient checkpointing 冲突
节省训练显存
```

KV cache 主要用于推理加速，训练时通常关闭。

---

## 12.10 prepare_model_for_kbit_training

```python
model = prepare_model_for_kbit_training(model)
```

作用：

```text
为 k-bit 量化训练准备模型
```

它通常会做：

```text
冻结基础模型参数
处理 layer norm 精度
允许输入 embedding 计算梯度
适配量化模型训练
```

这是 QLoRA 训练的重要步骤。

---

## 12.11 LoraConfig

```python
lora_config = LoraConfig(
    r=8,
    lora_alpha=16,
    target_modules=[
        "q_proj",
        "k_proj",
        "v_proj",
        "o_proj",
        "gate_proj",
        "up_proj",
        "down_proj"
    ],
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM"
)
```

作用：

```text
定义 LoRA adapter 插入哪些层、秩是多少、缩放系数是多少
```

---

### target_modules

```python
target_modules=[
    "q_proj",
    "k_proj",
    "v_proj",
    "o_proj",
    "gate_proj",
    "up_proj",
    "down_proj"
]
```

这些是 Transformer 中常见的线性层。

| 模块 | 作用 |
|---|---|
| q_proj | 生成 Query 向量 |
| k_proj | 生成 Key 向量 |
| v_proj | 生成 Value 向量 |
| o_proj | 注意力输出投影 |
| gate_proj | MLP 门控投影 |
| up_proj | MLP 升维投影 |
| down_proj | MLP 降维投影 |

---

## 12.12 get_peft_model

```python
model = get_peft_model(model, lora_config)
```

作用：

```text
根据 LoRA 配置，把 LoRA adapter 插入基础模型
```

执行后：

```text
原模型大部分参数被冻结
只有 LoRA 参数可训练
```

---

## 12.13 print_trainable_parameters

```python
model.print_trainable_parameters()
```

作用：

```text
打印可训练参数数量和总参数数量
确认 LoRA 是否正确生效
```

输出通常类似：

```text
trainable params: xxx || all params: xxx || trainable%: 0.x%
```

如果 trainable% 很小，说明 LoRA 生效。

---

# 13. TrainingArguments 参数解析

代码：

```python
training_args = TrainingArguments(
    output_dir=output_dir,
    num_train_epochs=5,
    per_device_train_batch_size=1,
    gradient_accumulation_steps=8,
    learning_rate=2e-4,
    weight_decay=0.01,
    logging_steps=1,
    save_steps=20,
    save_total_limit=2,
    fp16=True,
    bf16=False,
    optim="paged_adamw_8bit",
    lr_scheduler_type="cosine",
    warmup_ratio=0.05,
    report_to="none",
    gradient_checkpointing=True,
    max_grad_norm=0.3
)
```

---

## 13.1 output_dir

```python
output_dir=output_dir
```

作用：

```text
指定训练输出目录
保存 checkpoint、LoRA adapter 和 tokenizer
```

---

## 13.2 num_train_epochs

```python
num_train_epochs=5
```

作用：

```text
训练数据被完整遍历多少轮
```

注意：

```text
小数据集 epoch 太多容易过拟合
```

对于几十条数据，建议：

```text
1 到 2 epoch
```

---

## 13.3 per_device_train_batch_size

```python
per_device_train_batch_size=1
```

作用：

```text
每张 GPU 每次前向传播使用多少条样本
```

8GB 显存建议保持为：

```text
1
```

---

## 13.4 gradient_accumulation_steps

```python
gradient_accumulation_steps=8
```

作用：

```text
梯度累积步数
```

因为 batch size 很小，可以通过梯度累积模拟更大的 batch。

有效 batch size 大致为：

$$\text{effective batch size} = \text{per device batch size} \times \text{gradient accumulation steps} \times \text{GPU 数量}$$

在本项目中：

$$1 \times 8 \times 1 = 8$$

---

## 13.5 learning_rate

```python
learning_rate=2e-4
```

作用：

```text
控制参数更新步长
```

LoRA 常用学习率比全参数微调略大。

但小数据集建议降低到：

```text
5e-5 或 1e-4
```

---

## 13.6 weight_decay

```python
weight_decay=0.01
```

作用：

```text
权重衰减
一种正则化方法
有助于减少过拟合
```

---

## 13.7 logging_steps

```python
logging_steps=1
```

作用：

```text
每隔多少训练 step 打印一次日志
```

小数据集可以设为 1，方便观察 loss。

---

## 13.8 save_steps

```python
save_steps=20
```

作用：

```text
每隔多少 step 保存一次 checkpoint
```

---

## 13.9 save_total_limit

```python
save_total_limit=2
```

作用：

```text
最多保留多少个 checkpoint
防止硬盘被占满
```

---

## 13.10 fp16

```python
fp16=True
```

作用：

```text
使用 FP16 混合精度训练
减少显存占用
提升训练速度
```

---

## 13.11 bf16

```python
bf16=False
```

作用：

```text
不使用 BF16
```

RTX 4060 通常 FP16 更稳妥。

---

## 13.12 optim

```python
optim="paged_adamw_8bit"
```

作用：

```text
使用 bitsandbytes 提供的分页 8-bit AdamW 优化器
```

优点：

```text
优化器状态更省显存
适合 QLoRA
```

---

## 13.13 lr_scheduler_type

```python
lr_scheduler_type="cosine"
```

作用：

```text
使用余弦学习率调度
训练过程中学习率逐渐变化
```

---

## 13.14 warmup_ratio

```python
warmup_ratio=0.05
```

作用：

```text
训练前 5% 步数逐渐升高学习率
避免训练初期不稳定
```

---

## 13.15 report_to

```python
report_to="none"
```

作用：

```text
不向 wandb、tensorboard 等外部平台报告训练日志
```

---

## 13.16 gradient_checkpointing

```python
gradient_checkpointing=True
```

作用：

```text
用计算换显存
减少训练时保存的中间激活
```

代价：

```text
训练速度变慢
但显存占用降低
```

---

## 13.17 max_grad_norm

```python
max_grad_norm=0.3
```

作用：

```text
梯度裁剪
防止梯度爆炸
提高训练稳定性
```

---

# 14. SFTTrainer 技术解析

代码：

```python
trainer = SFTTrainer(
    model=model,
    tokenizer=tokenizer,
    train_dataset=dataset,
    formatting_func=formatting_func,
    args=training_args,
    max_seq_length=1024,
    packing=False
)
```

---

## 14.1 SFT 是什么

SFT 是：

```text
Supervised Fine-Tuning
监督微调
```

LLM 中的 SFT 通常是：

```text
给模型用户问题
让模型学习输出标准答案
```

例如：

```text
用户：什么是 LoRA？
助手：LoRA 是 Low-Rank Adaptation...
```

---

## 14.2 SFTTrainer

`SFTTrainer` 来自 TRL 库。

作用：

```text
简化大语言模型监督微调流程
自动处理文本格式化、tokenization、训练循环等
```

---

## 14.3 formatting_func

```python
formatting_func=formatting_func
```

作用：

```text
告诉 SFTTrainer 如何把原始 JSON 数据变成训练文本
```

---

## 14.4 max_seq_length

```python
max_seq_length=1024
```

作用：

```text
限制每条训练样本最大 token 长度
超过会截断
```

8GB 显存建议：

```text
512 或 1024
```

如果 OOM，改为：

```text
512
```

---

## 14.5 packing

```python
packing=False
```

作用：

```text
不把多个短样本拼接成一个长序列
```

`packing=True` 可以提升训练效率，但初学阶段不建议开启，因为调试更复杂。

---

# 15. 训练与保存

## 15.1 开始训练

```python
trainer.train()
```

作用：

```text
启动完整训练循环
包括前向传播、loss 计算、反向传播、梯度累积、优化器更新、日志打印、checkpoint 保存
```

---

## 15.2 保存 LoRA adapter

```python
trainer.model.save_pretrained(output_dir)
tokenizer.save_pretrained(output_dir)
```

保存内容包括：

```text
LoRA adapter 权重
adapter 配置
tokenizer 文件
```

注意：

```text
这里保存的不是完整基础模型
只是 LoRA adapter
```

---

# 16. LoRA 推理代码解析

核心代码：

```python
from peft import PeftModel
```

`PeftModel` 用于把 LoRA adapter 加载到基础模型上。

---

## 16.1 加载 tokenizer

```python
tokenizer = AutoTokenizer.from_pretrained(
    lora_path,
    trust_remote_code=True
)
```

作用：

```text
从 LoRA 输出目录加载 tokenizer
```

---

## 16.2 加载基础模型

```python
base_model = AutoModelForCausalLM.from_pretrained(
    base_model_name,
    quantization_config=bnb_config,
    device_map="auto",
    trust_remote_code=True
)
```

作用：

```text
重新加载原始 Qwen2.5-3B-Instruct 基础模型
```

---

## 16.3 加载 LoRA adapter

```python
model = PeftModel.from_pretrained(
    base_model,
    lora_path
)
```

作用：

```text
将保存好的 LoRA adapter 挂载到基础模型上
```

最终模型等效为：

```text
基础模型 + LoRA 增量参数
```

---

## 16.4 交互式推理

`infer_lora_qwen.py` 加载完模型后，交互部分只有一行：

```python
run_chat_cli(
    model=model,
    tokenizer=tokenizer,
    system_prompt="你是一个专业、严谨、耐心的人工智能学习助手。",
    max_context_tokens=MAX_CONTEXT_TOKENS,
)
```

作用：

```text
进入多轮对话循环
维护对话历史，超预算时自动截断
支持 /help /clear /history /undo /system /tokens /save /load 等命令
输入 q / quit / exit 时退出
```

循环本身写在 `scripts/chat_cli.py`，上下文管理写在 `scripts/chat_session.py`，
三个推理脚本（FP16、4bit、LoRA）共用同一份实现。

历史为什么必须截断，见第 18 节。

---

# 17. 单轮推理函数 generate_response

这是最小可用的单轮推理写法，`infer_qwen.py` 用的就是这个形状：

```python
def generate_response(model, tokenizer, user_input):
    messages = [
        {
            "role": "system",
            "content": "你是一个专业、严谨、耐心的人工智能学习助手。"
        },
        {
            "role": "user",
            "content": user_input
        }
    ]

    text = tokenizer.apply_chat_template(
        messages,
        tokenize=False,
        add_generation_prompt=True
    )

    inputs = tokenizer(
        [text],
        return_tensors="pt"
    ).to(model.device)

    with torch.no_grad():
        outputs = model.generate(
            **inputs,
            max_new_tokens=512,
            do_sample=True,
            temperature=0.7,
            top_p=0.9,
            repetition_penalty=1.05
        )

    generated_ids = outputs[0][inputs["input_ids"].shape[-1]:]
    response = tokenizer.decode(
        generated_ids,
        skip_special_tokens=True
    )

    return response
```

流程：

```text
用户输入
  ↓
构造 messages
  ↓
套用 chat template
  ↓
tokenizer 编码
  ↓
模型 generate
  ↓
截取新生成 token
  ↓
decode 成文本
```

---

这个函数每次只送一条 `user` 消息，模型完全不知道上一轮说过什么，问一句答一句。

多轮版本把它拆成了两半：

```text
messages 的构造与截断  →  ChatSession（chat_session.py）
编码 / generate / 解码 →  generate_answer（chat_cli.py）
```

拆开的好处是：拼历史的逻辑只和 tokenizer 有关，和模型怎么加载无关，所以三个推理脚本可以共用。

---

# 18. KV cache 与上下文长度

12.9 里提到训练时要关掉 `use_cache`，推理时它默认是开着的。这一节解释 KV cache 到底是什么、它和上下文长度是什么关系，以及为什么 `chat_session.py` 要给对话历史设一个 token 预算。

---

## 18.1 生成一个 token 需要做什么

大模型是自回归生成的：

```text
输入：你 好 吗
  ↓ 前向一次
预测出：我
  ↓ 把"我"接到输入后面
输入：你 好 吗 我
  ↓ 前向一次
预测出：很
  ↓
...
```

也就是说，每生成一个新 token，模型都要把"目前已有的全部 token"再送进网络一次。

---

## 18.2 朴素做法的浪费

在每一层 attention 里，每个 token 都会被投影成 Q、K、V 三个向量。

关键点：因为是 causal attention（前面的 token 看不到后面的），已有 token 的 K、V 和上一步算出来的**完全一样**。

```text
第 1 步：算 3 个 token 的 K/V
第 2 步：算 4 个 token 的 K/V   ← 前 3 个是重复计算
第 3 步：算 5 个 token 的 K/V   ← 前 4 个是重复计算
```

生成越长，重复计算越多。

---

## 18.3 KV cache 是什么

KV cache 就是把每一层算过的 K、V 存下来，下一步只算新 token 的 K、V，再和缓存拼接。

```text
不用 cache：每步都要前向 n 个 token
用   cache：每步只前向 1 个 token，其余 n-1 个 token 的 K/V 直接读缓存
```

这是推理提速最重要的一个优化，代价是显存。

---

## 18.4 KV cache 占多少显存

计算公式：

```text
每个 token 的 KV 显存
= 2（K 和 V 各一份）
× 层数
× KV 头数
× head_dim
× 每个数占的字节数
```

Qwen2.5-3B-Instruct 的配置（可以在模型目录的 `config.json` 里核对）：

```text
num_hidden_layers    = 36
num_attention_heads  = 16
num_key_value_heads  = 2      ← GQA，K/V 头数远少于 Q 头数
head_dim             = 2048 / 16 = 128
推理精度 fp16        = 2 字节
```

代入：

```text
2 × 36 × 2 × 128 × 2 = 36864 字节 ≈ 36 KB / token
```

不同上下文长度对应的 KV cache：

| 上下文长度 | KV cache 大小 |
|---|---|
| 2048 | 约 72 MB |
| 4096 | 约 144 MB |
| 8192 | 约 288 MB |
| 32768（模型位置上限） | 约 1.1 GB |

对比一下：3B 模型 4bit 权重大约 2 GB。

```text
2048 token 的对话：KV cache 只有几十 MB，完全不是瓶颈
32k token 的长文：KV cache 已经和模型权重一个量级
```

所以短对话不用担心，真正拉长上下文时 KV cache 才是主要开销。

---

## 18.5 GQA 让 KV cache 小了 8 倍

Qwen2.5 用的是 GQA（Grouped Query Attention）：16 个 Q 头共享 2 组 K/V 头。

如果换成传统 MHA（16 组 K/V）：

```text
2 × 36 × 16 × 128 × 2 = 294912 字节 ≈ 288 KB / token
2048 token → 约 576 MB，是现在的 8 倍
```

现在的主流模型几乎都用 GQA 或 MQA，省的不是权重显存，而是 KV cache。

---

## 18.6 prefill 与 decode 两个阶段

一次生成分成两段：

```text
prefill（预填充）：把整个 prompt 一次性送进模型，一口气算完所有 token 的 K/V
decode （解码）  ：之后每次只算 1 个新 token
```

两个阶段的性能特点不一样：

```text
prefill：计算密集，耗时随 prompt 长度增长
decode ：每步计算量固定，但要读整个 KV cache，受显存带宽限制
```

对应到用户的体感：

```text
prompt 越长   → 第一个字出来越慢（首 token 延迟高）
上下文越长   → 后面每个字吐得越慢
```

---

## 18.7 这和多轮对话的关系

本项目每一轮都是把 `[system] + 全部历史 + 新问题` 重新拼成一个字符串，重新走一次 prefill：

```text
第 1 轮 prompt：约 50 token    → prefill 50
第 5 轮 prompt：约 800 token   → prefill 800
第 20 轮 prompt：约 4000 token → prefill 4000
```

如果历史完全不截断：

```text
显存占用随轮数线性上涨
首 token 延迟随轮数线性上涨
最终要么慢到不能用，要么超过模型 32768 的位置上限直接报错
```

这就是 `chat_session.py` 里 `max_context_tokens` 的意义：给**输入侧**设一个上限，超了就从最旧的一轮开始丢。

---

## 18.8 max_context_tokens 与 max_new_tokens

这两个参数管的是两头，不要混：

```text
max_context_tokens：prompt（system + 历史 + 新问题）最多多少 token
                    由 ChatSession.trim() 控制
max_new_tokens    ：这一轮最多生成多少 token
                    传给 model.generate
```

两者之和不能超过模型的最大位置长度：

```text
2048 + 512 = 2560  <  32768   安全
```

峰值 KV cache 也由两者之和决定：

```text
(2048 + 512) × 36 KB ≈ 90 MB
```

---

## 18.9 为什么训练时要关掉 use_cache

训练时是把整条样本一次性送进去算 loss，不做逐 token 的自回归生成，K/V 算完就没用了，缓存毫无意义。

而且它会和 gradient checkpointing 冲突：

```text
KV cache            ：想把中间结果留着
gradient checkpointing：想把中间结果丢掉，反向时重算
```

两个目标相反，所以 12.9 里写：

```python
model.config.use_cache = False
```

推理脚本里不需要管，`model.generate` 默认就开着 KV cache。

---

## 18.10 本项目没做的：跨轮复用 KV cache

第 2 轮的 prompt，其实是第 1 轮 prompt 的前缀加上新内容。理论上第 1 轮的 KV cache 可以留着，第 2 轮只 prefill 新增的那一小段。

这个优化叫 prefix caching，vLLM、SGLang 这类推理框架都做了。本项目没做，原因是：

```text
需要自己管理 past_key_values 的生命周期
一旦从头部丢弃历史，前缀就变了，整个缓存作废
代码复杂度明显上升，不利于阅读
```

顺带一提：`trim()` 从最旧的一轮开始丢，恰好是对 prefix caching 最不友好的策略——前缀一变，缓存全废。工业界的做法是尽量保住前缀，比如只压缩中间段落，或者把旧历史摘要后塞进 system prompt。这属于进阶话题，本项目优先保证代码好读。

---

# 19. 幻觉问题分析

## 19.1 什么是幻觉

LLM 幻觉是指模型生成了看似合理但事实错误的内容。

例如：

```text
LoRA 是 Lightweight Recursive Operator Allocation 的缩写
```

这是错误的。

正确是：

```text
LoRA = Low-Rank Adaptation
```

---

## 19.2 为什么会幻觉

可能原因：

```text
模型参数规模较小
训练数据不足
推理采样随机性较高
微调数据质量差
过拟合
知识被微调带偏
模型本身没有事实校验机制
```

---

## 19.3 如何降低幻觉

方法包括：

```text
使用更高质量训练数据
降低 temperature
使用 do_sample=False
增加事实性样本
减少过高 epoch
降低学习率
使用 RAG 检索增强
进行评测集验证
```

对于知识问答，可以使用：

```python
outputs = model.generate(
    **inputs,
    max_new_tokens=512,
    do_sample=False,
    repetition_penalty=1.05
)
```

---

# 20. 常见报错与排查

## 20.1 CUDA 不可用

检查：

```bash
nvidia-smi
```

检查 PyTorch：

```bash
python -c "import torch; print(torch.cuda.is_available())"
```

如果是 False，可能原因：

```text
NVIDIA 驱动未安装
PyTorch CUDA 版本不匹配
当前环境不是 qwen_lora
```

---

## 20.2 CUDA out of memory

报错：

```text
torch.cuda.OutOfMemoryError
```

解决：

```text
max_seq_length 从 1024 改为 512
r 从 8 改为 4
target_modules 减少为 q_proj、k_proj、v_proj、o_proj
per_device_train_batch_size 保持 1
关闭其他 GPU 进程
使用 4-bit 加载
```

查看 GPU：

```bash
nvidia-smi
```

---

## 20.3 bitsandbytes 报错

检查：

```bash
python -m bitsandbytes
```

升级：

```bash
pip install -U bitsandbytes
```

---

## 20.4 HuggingFace 下载慢

设置镜像：

```bash
export HF_ENDPOINT=https://hf-mirror.com
```

永久写入：

```bash
echo 'export HF_ENDPOINT=https://hf-mirror.com' >> ~/.bashrc
source ~/.bashrc
```

---

## 20.5 SFTTrainer 参数不兼容

可能原因：

```text
trl 版本不同
transformers 版本不同
```

升级：

```bash
pip install -U trl transformers peft accelerate
```

如果 `max_seq_length` 报错，可以删除该参数或改用新版 `SFTConfig`。

---

# 21. 微调实验建议

## 21.1 小数据集只能跑通流程

几十条数据只能用于学习：

```text
数据格式
训练流程
LoRA 保存
LoRA 加载
推理流程
```

不能指望显著提升模型能力。

---

## 21.2 更合理的数据规模

建议：

| 数据量 | 可能效果 |
|---:|---|
| 20 条 | 跑通流程 |
| 500 条 | 可能看到风格变化 |
| 2000 条 | 有较明显领域适配 |
| 10000 条以上 | 可以做初步领域助手 |

---

## 21.3 推荐训练参数

对于 RTX 4060 8GB：

```python
per_device_train_batch_size=1
gradient_accumulation_steps=8
max_seq_length=512
r=4 或 8
learning_rate=5e-5 到 1e-4
num_train_epochs=1 到 3
```

---

# 22. 各工具之间的关系

```text
Ollama
    适合快速部署和体验

Transformers
    适合手动加载模型、理解推理流程

PyTorch
    提供张量计算和 GPU 支持

bitsandbytes
    提供 4-bit/8-bit 量化和低显存优化器

PEFT
    提供 LoRA、QLoRA 等参数高效微调方法

TRL
    提供 SFTTrainer，简化 LLM 监督微调

HuggingFace Datasets
    负责加载和处理训练数据
```

---

# 23. 本项目核心知识总结

通过本项目，你应该理解：

1. Ollama 是快速部署工具，不是深入训练框架；
2. Transformers 可以手动加载和推理 HuggingFace 模型；
3. Tokenizer 负责文本和 token ID 的转换；
4. Chat template 决定聊天模型的输入格式；
5. CausalLM 通过预测下一个 token 生成文本；
6. 4-bit 量化可以显著降低显存占用；
7. LoRA 通过低秩矩阵训练少量参数；
8. QLoRA 是 4-bit 量化模型上的 LoRA 微调；
9. LoRA adapter 不是完整模型，需要和基础模型一起使用；
10. 小数据微调不一定提升模型，可能导致幻觉；
11. 推理参数会显著影响输出稳定性；
12. 微调质量主要取决于数据质量、训练参数和评测方法。

---

# 24. 推荐后续学习方向

完成本项目后，可以继续学习：

```text
1. 手写一个最小 Transformer
2. 理解 attention、MLP、LayerNorm、RoPE
3. 学习 tokenizer 训练方法
4. 学习 LoRA target_modules 的选择
5. 学习 assistant-only loss masking
6. 学习 RAG 检索增强生成
7. 学习模型评测方法
8. 学习合并 LoRA 权重
9. 学习 GGUF 转换与 llama.cpp 推理
10. 学习多轮对话数据构造
```

---

# 25. 附录：关键命令汇总

## Ollama

```bash
curl -fsSL https://ollama.com/install.sh | sh
ollama --version
ollama run qwen2.5:3b
ollama list
```

---

## Conda 环境

```bash
conda create -n qwen_lora python=3.10 -y
conda activate qwen_lora
```

---

## 安装依赖

```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install transformers datasets accelerate peft trl bitsandbytes sentencepiece protobuf scipy scikit-learn tensorboard tqdm
```

---

## 检查 GPU

```bash
nvidia-smi
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
```

---

## 推理

```bash
# 单轮最小示例
python ~/llm-learning/scripts/infer_qwen.py

# 多轮对话（基座模型）
python ~/llm-learning/scripts/chat_qwen.py
python ~/llm-learning/scripts/chat_qwen.py --load_in_4bit
```

---

## 训练

```bash
cd ~/llm-learning
python scripts/train_qlora_qwen.py
```

---

## LoRA 推理

```bash
python scripts/infer_lora_qwen.py
```

---

# 26. 最终说明

本项目的价值不在于得到一个非常强的微调模型，而在于完整打通了：

```text
本地部署
手写推理
数据构造
QLoRA 训练
adapter 保存
adapter 加载
微调效果分析
```

这是一条非常适合个人电脑学习 LLM 工程和微调技术的路线。

对于你的硬件条件，Qwen2.5-3B-Instruct + QLoRA 是一个合理的起点。

后续如果要真正提升模型能力，关键不在于重复跑训练，而在于：

```text
构造高质量数据
控制训练参数
建立评测集
对比微调前后效果
分析错误案例
```
